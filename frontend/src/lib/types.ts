/** Re-export shared types from canonical location (see .cursor/rules/frontend.mdc) */
export type {
  AdminSettings,
  Post,
  PostAdminSummary,
  PostEditorData,
  PostInsight,
  PostStatus,
  PostTag,
  SeoDefaults,
  SeoHealth
} from "@/types";

